{{- include "mediamtx.validate" . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mediamtx.fullname" . }}
  labels:
    {{- include "mediamtx.labels" . | nindent 4 }}
data:
  mediamtx.yml: |
    {{- include "mediamtx.config" . | nindent 4 }}
